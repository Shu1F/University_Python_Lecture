# import pandas as pd
# import seaborn as sns
# import matplotlib.pyplot as plt
# import japanize_matplotlib

# new_file_path = "A班.csv"  # 適宜変更

# new_data = pd.read_csv(new_file_path, encoding="shift-jis")

# data_full = new_data.iloc[2:]
# data_full = data_full.apply(pd.to_numeric, errors="coerce")
# data_with_fuel_full = data_full[data_full["燃料噴射量"] > 0]
# data_with_fuel_full["燃費"] = (
#     data_with_fuel_full["走行距離"] / data_with_fuel_full["燃料噴射量"]
# )

# correlation_full = data_with_fuel_full.corr()

# plt.figure(figsize=(20, 15))
# sns.heatmap(correlation_full, annot=False, cmap="coolwarm")
# plt.title("A班のヒートマップ")
# plt.show()


# new_file_path = "B班.csv"  # 適宜変更

# new_data = pd.read_csv(new_file_path, encoding="shift-jis")

# data_full = new_data.iloc[2:]
# data_full = data_full.apply(pd.to_numeric, errors="coerce")
# data_with_fuel_full = data_full[data_full["燃料噴射量"] > 0]
# data_with_fuel_full["燃費"] = (
#     data_with_fuel_full["走行距離"] / data_with_fuel_full["燃料噴射量"]
# )

# correlation_full = data_with_fuel_full.corr()

# plt.figure(figsize=(20, 15))
# sns.heatmap(correlation_full, annot=False, cmap="coolwarm")
# plt.title("B班のヒートマップ")
# plt.show()

# new_file_path = "C班.csv"  # 適宜変更

# new_data = pd.read_csv(new_file_path, encoding="shift-jis")

# data_full = new_data.iloc[2:]
# data_full = data_full.apply(pd.to_numeric, errors="coerce")
# data_with_fuel_full = data_full[data_full["燃料噴射量"] > 0]
# data_with_fuel_full["燃費"] = (
#     data_with_fuel_full["走行距離"] / data_with_fuel_full["燃料噴射量"]
# )

# correlation_full = data_with_fuel_full.corr()

# plt.figure(figsize=(20, 15))
# sns.heatmap(correlation_full, annot=False, cmap="coolwarm")
# plt.title("C班のヒートマップ")
# plt.show()

# new_file_path = "D班.csv"  # 適宜変更

# new_data = pd.read_csv(new_file_path, encoding="shift-jis")

# data_full = new_data.iloc[2:]
# data_full = data_full.apply(pd.to_numeric, errors="coerce")
# data_with_fuel_full = data_full[data_full["燃料噴射量"] > 0]
# data_with_fuel_full["燃費"] = (
#     data_with_fuel_full["走行距離"] / data_with_fuel_full["燃料噴射量"]
# )

# correlation_full = data_with_fuel_full.corr()

# plt.figure(figsize=(20, 15))
# sns.heatmap(correlation_full, annot=False, cmap="coolwarm")
# plt.title("D班のヒートマップ")
# plt.show()


# new_file_path = "E班.csv"  # 適宜変更

# new_data = pd.read_csv(new_file_path, encoding="shift-jis")

# data_full = new_data.iloc[2:]
# data_full = data_full.apply(pd.to_numeric, errors="coerce")
# data_with_fuel_full = data_full[data_full["燃料噴射量"] > 0]
# data_with_fuel_full["燃費"] = (
#     data_with_fuel_full["走行距離"] / data_with_fuel_full["燃料噴射量"]
# )

# correlation_full = data_with_fuel_full.corr()

# plt.figure(figsize=(20, 15))
# sns.heatmap(correlation_full, annot=False, cmap="coolwarm")
# plt.title("E班のヒートマップ")
# plt.show()


# new_file_path = "F班.csv"  # 適宜変更

# new_data = pd.read_csv(new_file_path, encoding="shift-jis")

# data_full = new_data.iloc[2:]
# data_full = data_full.apply(pd.to_numeric, errors="coerce")
# data_with_fuel_full = data_full[data_full["燃料噴射量"] > 0]
# data_with_fuel_full["燃費"] = (
#     data_with_fuel_full["走行距離"] / data_with_fuel_full["燃料噴射量"]
# )

# correlation_full = data_with_fuel_full.corr()

# plt.figure(figsize=(20, 15))
# sns.heatmap(correlation_full, annot=False, cmap="coolwarm")
# plt.title("F班のヒートマップ")
# plt.show()
